server:
  port: 8081
spring:
    application:
      name: student
#    profiles:
#      active: dev
    cloud:
      nacos:
        discovery:
          server-addr: 127.0.0.1:8848
        config:
          server-addr: localhost:8848
          file-extension: yml